Задача с LeetCode, которая МОЖЕТ БЫТЬ решено при помощи Map:

Даны две строки s и t, возвращает true если t является анаграммой s или false в противном случае.
Анаграммы — это пары слов, составленных из одного набора букв.

Example 1:
    Input: s = "anagram", t = "nagaram"
    Output: true

Example 2:
    Input: s = "rat", t = "car"
    Output: false

Подумайте как эту задачу можно решить при использовании Map.
(Писать не надо, оно занимает время и требует знать синтаксис и функции Map)