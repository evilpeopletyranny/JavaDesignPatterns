1.  Прочитать файл task2Text.
2.  Заменить все вхождения слова Джек на ваше имя.

    Для этого вам поможет функция String.replace() или String.replaceAll()
    В качестве первого параметра принимается строка/регекс дл замены
    В качестве второго параметра принимается значение на которое будет произведена замена.

3. Записать это в НОВЫЙ ФАЙЛ - название сами придумайте.